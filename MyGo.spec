# -*- mode: python ; coding: utf-8 -*-
"""PyInstaller recipe for the native MyGo.app bundle."""


a = Analysis(
    ["mygo_app.py"],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[
        "optimum",
        "pytest",
        "scipy",
        "sentence_transformers",
        "sklearn",
        "tensorflow",
        "tkinter",
        "torch",
        "transformers",
    ],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name="MyGo",
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    console=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=False,
    name="MyGo",
)
